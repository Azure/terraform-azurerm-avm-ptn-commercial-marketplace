# Examples

- Create a directory for each example.
- Keep examples deployable without interactive input.
- Ensure examples are idempotent where possible.
- Use `.e2eignore` only when a justified exception is required.
