# Ignore this example in the e2e tests
# This example requires pre-existing Azure AD App Registration IDs
# (fulfillment_app_id, fulfillment_app_secret, admin_app_id, portal_app_id)
# which cannot be provisioned automatically in CI/CD pipelines.
